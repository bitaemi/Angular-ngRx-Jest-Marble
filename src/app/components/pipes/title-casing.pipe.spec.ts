import { TitleCasingPipe } from './title-casing.pipe';

describe('TitleCasingPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCasingPipe();
    expect(pipe).toBeTruthy();
  });
});
